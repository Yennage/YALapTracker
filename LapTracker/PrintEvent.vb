Public Class PrintEvent

    Public Sub PrintEvent(ByVal amID As String, ByVal pmID As String)

        ' This will handle printing the event details to both an HTML table and a CSV file

    End Sub
End Class
